// import the resolvers
const resolvers = require('./resolvers');
// import the type definitions
const typeDefs = require('./typeDefs');

// export both the resolvers and type definitions
module.exports = { typeDefs, resolvers };